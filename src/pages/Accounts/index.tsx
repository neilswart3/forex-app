import { AccountsList } from 'src/components'
import * as Styled from './styles'

const Accounts: React.FC = () => {
  return (
    <Styled.Accounts title='Accounts'>
      <AccountsList />
    </Styled.Accounts>
  )
}

export default Accounts
