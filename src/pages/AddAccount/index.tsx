import * as Styled from './styles'

const AddAccount: React.FC = () => {
  return (
    <Styled.AddAccount title='Add Account'>
      AddAccount component
    </Styled.AddAccount>
  )
}

export default AddAccount
